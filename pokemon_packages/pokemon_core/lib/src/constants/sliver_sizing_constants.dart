import 'package:flutter/material.dart';

//translates height by 2 logical pixels
const sliverHeight2 = SliverToBoxAdapter(child: SizedBox(height: 2));

//translates height by 4 logical pixels
const sliverHeight4 = SliverToBoxAdapter(child: SizedBox(height: 4));

//translates height by 8 logical pixels
const sliverHeight8 = SliverToBoxAdapter(child: SizedBox(height: 8));

//translates height by 12 logical pixels
const sliverHeight12 = SliverToBoxAdapter(child: SizedBox(height: 12));

//translates height by 16 logical pixels
const sliverHeight16 = SliverToBoxAdapter(child: SizedBox(height: 16));
